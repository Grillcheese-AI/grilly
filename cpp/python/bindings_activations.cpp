/// bindings_activations.cpp — Activation function bindings (forward + backward).
///
/// Stub: will be populated with relu, gelu, silu, tanh_act, softmax,
/// and their backward variants from the monolithic bindings.cpp.

#include "bindings_core.h"

void register_activations_ops(py::module_& m) {
    // TODO: Move activation ops from bindings.cpp
    (void)m;
}
