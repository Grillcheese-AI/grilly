/// bindings_linear.cpp — Linear layer op bindings (forward + backward).
///
/// Stub: will be populated with linear, linear_cpu, linear_backward
/// from the monolithic bindings.cpp in a later task.

#include "bindings_core.h"

void register_linear_ops(py::module_& m) {
    // TODO: Move linear, linear_cpu, linear_backward from bindings.cpp
    (void)m;
}
